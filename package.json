{
  "name": "subgraph1",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ onurcanakkoc/subgraph1",
    "create-local": "graph create --node http://localhost:8020/ onurcanakkoc/subgraph1",
    "remove-local": "graph remove --node http://localhost:8020/ onurcanakkoc/subgraph1",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 onurcanakkoc/subgraph1",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.1",
    "@graphprotocol/graph-ts": "0.27.0",
    "@protofire/subgraph-toolkit": "^0.1.2"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
